version: '2'
services:
  python-dev-nvidia:
    image: nmirinagoyansaito/python-dev-nvidia
    build:
      context: python-dev-nvidia
      args:
        - USER
    stdin_open: true
    tty: true
    volumes:
       - ./workdir:/home/${USER}/workdir
